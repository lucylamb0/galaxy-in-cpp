//
// Created by User on 31/07/2022.
//

#ifndef C_VERSION_INCLUDES_H
#define C_VERSION_INCLUDES_H

#include <cmath>
#include <vector>
#include <iostream>

const double PI = 3.14159265358979323846;
const double gravitationalConstant = 6.67408 * pow(10, -11);
const double starMass = 1.98855 * pow(10, 30);
const double starRadius = 6.957 * pow(10, 8);
const double starDistance = 1.496 * pow(10, 11);
const double starTemperature = 5778;
const double starLuminosity = 3.828 * pow(10, 26);
const double starDiameter = 6.957 * pow(10, 8);
const double starSurfaceGravity = 0.006674;
const double starSurfacePressure = 0.000002;
const double starSurfaceTemperature = 5778;
const double starSurfaceLuminosity = 3.828 * pow(10, 26);
const double starSurfaceRadius = 6.957 * pow(10, 8);
const double starSurfaceDiameter = 6.957 * pow(10, 8);
const double starSurfaceVolume = 4.18879 * pow(10, 18);
const double starSurfaceArea = 5.670 * pow(10, -8);
const double starSurfaceMass = 1.98855 * pow(10, 30);
const double parsec = 3.08567758 * pow(10, 16);
const double parsec_per_year = 9.785 * pow(10, 8);

const double yearInSeconds = 31536000;
const double time_step = yearInSeconds * 10;

#endif //C_VERSION_INCLUDES_H
